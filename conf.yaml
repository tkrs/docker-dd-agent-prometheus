instances:
  # The prometheus endpoint to query from
  - prometheus_url: http://%%host%%:%%env_PROMETHEUS_PORT%%/
    namespace: %%env_NAMESPACE%%
    metrics:
      - jvm*
      - java*
      - process*
